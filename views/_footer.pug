footer

  
  .footer-wrapper.flex
    .content-footer.flex
      a(href="") Contact Me

      a(href="https://www.mattiayaghmai.com/") My Portfolio 

  -if( user && user.role === 'writer' || user.role === 'admin')
    .form-groupHeader.buttonsAccept.flex.flex-column
      button(class='buttonLogout logoutButton') 
        a.logoutButton Logout
    -console.log(user)
